# Ambiente
CRM_ENV=production
CRM_DEBUG=false

# Segurança (trocar por valor longo e aleatório)
CRM_SECRET_KEY=troque_por_uma_chave_longa_e_unica

# Persistência (Railway com volume em /data)
CRM_DATA_FILE=/data/data.json
CRM_UPLOAD_DIR=/data/uploads
